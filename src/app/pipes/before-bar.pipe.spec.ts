import { BeforeBarPipe } from './before-bar.pipe';

describe('BeforeBarPipe', () => {
  it('create an instance', () => {
    const pipe = new BeforeBarPipe();
    expect(pipe).toBeTruthy();
  });
});
